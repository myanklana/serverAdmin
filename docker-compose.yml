version: "3.9"

services:

  postgres:

    image: postgres:17

    environment:

      POSTGRES_DB: servermanager

      POSTGRES_USER: postgres

      POSTGRES_PASSWORD: postgres

    ports:

      - "5432:5432"